{
  "background_llm_provider": {
    "description": "后台小模型",
    "type": "string",
    "hint": "用于记忆编码判定、巩固升华、洞察归纳的模型，建议使用成本更低的模型。留空则回退使用当前对话模型。",
    "obvious_hint": true,
    "_special": "select_provider",
    "default": ""
  },
  "enable_private_memory": {
    "description": "启用私聊记忆",
    "type": "bool",
    "default": true,
    "hint": "关闭后所有私聊不进行记忆捕获与召回；可在 WebUI「记忆管理 → 会话配置」中按会话单独覆盖"
  },
  "enable_group_memory": {
    "description": "启用群聊记忆",
    "type": "bool",
    "default": true,
    "hint": "关闭后所有群聊不进行记忆捕获与召回；可在 WebUI「记忆管理 → 会话配置」中按会话单独覆盖"
  },
  "consolidation_scan_interval_minutes": {
    "description": "巩固扫描周期（分钟）",
    "type": "int",
    "hint": "后台周期性扫描所有会话，检查是否满足批量抽取条件",
    "default": 30
  },
  "consolidation_count_threshold_private": {
    "description": "私聊抽取触发阈值（原始轮次数）",
    "type": "int",
    "hint": "积攒的未抽取原始对话轮次达到该数量后触发一次批量抽取（每次触发只调 1 次后台模型）；可在 WebUI 中按会话覆盖",
    "default": 20
  },
  "consolidation_count_threshold_group": {
    "description": "群聊抽取触发阈值（原始轮次数）",
    "type": "int",
    "default": 50,
    "hint": "；可在 WebUI 中按会话覆盖"
  },
  "consolidation_idle_hours": {
    "description": "巩固触发的静默时长（小时）",
    "type": "int",
    "hint": "距上次巩固超过该时长，且存在未处理的新记忆时，即使未达数量阈值也会触发巩固；可在 WebUI 中按会话覆盖",
    "default": 12
  },
  "recall_top_k": {
    "description": "单次召回注入的记忆条数上限",
    "type": "int",
    "default": 5
  },
  "recall_core_top_k": {
    "description": "常驻记忆注入条数",
    "type": "int",
    "hint": "每次对话固定注入的语义记忆/洞察条数（按重要度×强度排序），模拟「对一个人的稳定认知」，0 表示关闭常驻层",
    "default": 3
  },
  "recall_recent_turns": {
    "description": "群聊近因层注入条数",
    "type": "int",
    "hint": "群聊召回时注入最近几条原始对话，补上群聊没有会话历史的短板，0 表示关闭（私聊已有会话历史，不受此项影响）",
    "default": 5
  },
  "enable_cross_scope_bridge": {
    "description": "允许群聊记忆桥接进私聊",
    "type": "bool",
    "hint": "开启后，已授权用户在群聊中的自我陈述会同时写入其私聊记忆；可在 WebUI 中按群聊单独开关。默认关闭。",
    "default": false
  },
  "bridge_max_sensitivity": {
    "description": "桥接允许的最高敏感度",
    "type": "string",
    "hint": "low：仅日常事实；medium：含一般隐私；high：不限制（不建议）；可在 WebUI 中按会话覆盖",
    "options": [
      "low",
      "medium",
      "high"
    ],
    "default": "low"
  },
  "group_capture_ignored_users": {
    "description": "群聊捕获忽略的用户",
    "type": "list",
    "hint": "名单内的发送者 ID，其群聊消息不落库、不参与记忆",
    "default": []
  },
  "group_capture_ignored_keywords": {
    "description": "群聊捕获忽略的关键词",
    "type": "list",
    "hint": "群聊消息包含任一关键词（子串匹配）时不落库",
    "default": []
  },
  "decay_rate_semantic": {
    "description": "语义记忆衰减系数",
    "type": "float",
    "hint": "语义记忆是已巩固的稳定认知，按距上次激活（写入/更新/被召回）的自然日数指数衰减，每天保留该比例（0-1），越小衰减越快；与巩固扫描周期无关。",
    "default": 0.98
  },
  "decay_rate_insight": {
    "description": "洞察衰减系数",
    "type": "float",
    "hint": "洞察是巩固后的精华，几乎不应被遗忘。",
    "default": 0.995
  },
  "raw_retention_days": {
    "description": "原始对话保留天数",
    "type": "int",
    "hint": "原始对话轮次超过该天数后物理删除（真正遗忘），0 表示不按时间清理；同时受单会话容量上限约束（每会话仅保留最近 500 条，消息量大的会话实际保留窗口可能短于该天数）",
    "default": 14
  }
}
