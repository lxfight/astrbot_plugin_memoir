# 更新日志

记录用户可感知的功能、行为变化与修复。已发布版本以 Git 标签为准；`Unreleased` 为该标签之后的主分支改动，不代表新的正式发布。

## [Unreleased]

## [0.5.0] — 2026-09-08

### WebUI 管理体验

- 调整为记忆、原文对话、处理任务、会话设置四个页面；全局设置和桥接授权有独立入口，无会话也能配置。
- 增加记忆/原文搜索筛选及完整分页；转发按原始事件聚合，支持层级展开、来源定位和全文复制，统计区分事件与分块。
- 支持人工编辑正文、标签和重要度，保留来源属性并校验内容版本；支持当前页批量删除、明确级联范围、操作后同步统计与分页。
- 展示配置实际生效值、覆盖来源、未保存状态与切换保护；处理任务增加中文建议和有条件的自动刷新。
- 采用小仓鼠品牌图标与轻量卡片布局，移除背景粒子；手机端改用会话抽屉，完善弹窗、键盘与明暗主题体验。
- 列表和设置面板随可用宽度铺开，消除宽屏两侧过大留白；以 1920×1080 验证明暗主题并更新预览截图。
- 新增查询、范围校验、编辑冲突、批量删除、移动端及异步操作回归测试。

### 新增

- 支持递归处理多层转发：通用 Node/Nodes、OneBot 引用拉取、Satori 内嵌结构，以及 Telegram/Discord 已交付正文和来源；卡片预览、飞书等无法展开的内容明确标注限制。
- 转发解析复用持久化后台队列，按适配器实例恢复；限制深度、内容、动作调用、超时与媒体预算，保留部分结果并提供可恢复任务重试。
- 长转发按节点分块保存、检索并关联原始事件，保留未验证的原作者、原时间与节点路径；WebUI 展示解析状态、来源详情和引用记忆标记。
- 引用资料与普通对话分批巩固，使用独立记忆键和来源标记，不覆盖个人画像、不进入常驻认知，也不参与跨会话桥接。

### 优化

- 巩固数量阈值按原始事件去重；转发分块随父事件删除、保留期和容量清理，删除分块会取消对应解析任务，防止重试恢复已删内容。
- 增加转发解析、适配器路由、重启、超时、隐私过滤、重试、来源隔离及手机端展示的自动回归测试。
- 精简 README，将详细配置、调用成本、开发发布流程移入独立文档，补充多层转发支持范围和操作说明。

### 使用说明

- 转发处理用于后续记忆和召回，不保证当前回复立即理解全文；包含转发的整条入站消息（含附言）均按引用处理。
- 新功能跟随现有会话记忆开关，升级后自动迁移数据库，仅处理新消息。

## [0.4.1] — 2026-09-08

### 文档与提示

- 在安装说明和配置界面明确提示群聊记忆默认开启、无需触发回复也可能记录消息，以及原文和媒体的模型处理方式。
- 修正「每次触发只调 1 次模型」的费用提示，说明巩固分批、失败重试、媒体解析及召回 Token 的费用影响。
- 明确关闭记忆不会清空已有数据，以及全局关闭优先于会话配置。

## [0.4.0] — 2026-09-07

### 新增

- 插件图标：原创「记忆小仓鼠」抱着青绿色记忆本；提供 256×256 `logo.png` 和可编辑 SVG 源文件。
- README 增加快速上手、明暗主题预览、配置速查、常见问题与图标设计说明。
- 图片、语音及混合消息解析：按照所选模型的能力配置生成描述，纳入原文检索和巩固。
- 持久化媒体后台队列：两个工作者、队列与附件大小限制、超时控制、重启恢复及失败重试。
- WebUI 支持跟随、浅色、深色主题，保存偏好并同步同源标签页。
- WebUI 增加处理状态、失败原因、任务重试和记忆来源原文查看。
- 三层召回共享字符预算，支持会话独立配置，仅强化实际注入的记忆。
- `/memoir consent` 指令允许用户自助查看、开启或关闭群聊桥接授权。
- Chromium WebUI 自动回归，以及存储迁移、并发、事务、模型输出与接口隔离测试。
- 自动发布流水线校验标签、元数据和锁文件版本，从对应 CHANGELOG 段落生成 Release 正文，提供 ZIP、SHA256 校验和及手动重跑入口。

### 优化

- 巩固前检索相关旧记忆，减少模型需要读取的内容。
- 结构化记忆增加唯一键，并按会话、稳定人物 ID、记忆类型和键去重；旧数据保留历史身份标签。
- 检索结合精确短语、标签、人物匹配与模糊线索评分；群聊人物加权使用稳定 ID。
- 过期事件记忆采用事件日期与宽限期计算衰减，避免反复召回维持失效事实。
- 巩固增加批次字符预算与每轮会话数量限制，优先处理较大积压；扫描间隔修改后动态生效。
- 周期巩固使用捕获时保存的 AstrBot 会话标识选择模型；缺少标识的旧会话显示失败原因。
- WebUI 改善窄屏布局、触屏操作、主题对比度与异步加载行为。

### 修复

- 使用串行事务处理共享 SQLite 连接，防止并发交错与批次部分提交；异常和取消时回滚。
- 清空会话、关闭会话或删除来源后，丢弃已失效的后台结果，防止旧数据重新写回。
- 非法模型输出进入隔离状态，保留失败原因和重试入口，不再标记为抽取成功。
- 全局开关、会话开关、桥接授权和目标私聊配置共同约束后台写入。
- `raw_retention_days = 0` 正确关闭时间清理，同时保持容量限制。
- WebUI 迟到的请求不再覆盖新会话或新标签内容，保存与重试绑定实际展示的会话。
- 私聊召回按用户角色条目统计已可见轮次；巩固更新同步刷新标签。
- 适配器缺少群 ID 时跳过群聊捕获与召回。
- 异常媒体组件记录为可见失败；模型人物字段增加类型校验；未指定发言人时不再意外加权公共事实。

### 升级说明

- 本次为 0.4.0 正式版，包含自 `0.3.0.dev0` 以来的功能更新与可靠性修复。
- 重载插件时自动迁移数据库，包括稳定人物标识、会话版本和持久化任务；迁移保留已有记忆。
- 历史多媒体占位符没有原始媒体引用，无法追补识别；旧会话缺少模型会话标识时，可指定后台模型或产生新消息后重试。

## [0.3.0.dev0] — 2026-09-01

首个带 Git 发布标签的开发版本，基于 `f29985e`。

### 新增

- SQLite 长期记忆流程：私聊完整轮次、群聊被动消息及助手回复捕获，后台提炼语义记忆与洞察。
- 常驻、线索与群聊近因三层召回，支持原文检索和发言人相关记忆加权。
- 会话级配置、群聊忽略名单、关键词过滤与桥接授权管理。
- 青绿色 WebUI 管理页，支持查看、搜索、删除、会话配置和全局配置编辑。
- MIT 许可证、GitHub CI、问题与 PR 模板、标签驱动的预发布打包流程。

### 优化与修复

- 原文保留期与容量限制、结构化记忆容量淘汰，以及按自然日计算的记忆强度衰减。
- 巩固支持更新、删除与去重洞察，并对无法解析的模型输出重试和备份。
- 缓存会话配置，减少数据库往返；减少无效强度更新和重复上下文注入。
- 收窄模型巩固指令与原文数据混合造成的提示注入影响面。
- 拆分 WebUI 样式与 ES 模块，增加完整记忆流程回归测试。

[Unreleased]: https://github.com/lxfight/astrbot_plugin_memoir/compare/v0.4.1...HEAD
[0.4.1]: https://github.com/lxfight/astrbot_plugin_memoir/releases/tag/v0.4.1
[0.4.0]: https://github.com/lxfight/astrbot_plugin_memoir/releases/tag/v0.4.0
[0.3.0.dev0]: https://github.com/lxfight/astrbot_plugin_memoir/releases/tag/v0.3.0.dev0
